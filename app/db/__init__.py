# AI Ticket Analyzer - Database layer (SQLAlchemy ORM, sessions, migrations).
#
# This package is self-contained and does not depend on application settings,
# so migration tooling (Alembic) can import the ORM metadata without requiring
# runtime configuration such as the OpenAI API key.
