# Development & CI tooling
# Install with: pip install -r requirements.txt -r requirements-dev.txt
#
# Runtime/test dependencies live in requirements.txt. This file adds the
# static-analysis and coverage tooling used locally and in CI (Milestone M0.1).

ruff==0.8.4
mypy==1.14.0
pytest-cov==6.0.0
